import React, { useEffect } from 'react';
import ModelView from './view';

const Model = ({ navigation }) => {


  useEffect(() => { }, []);

  return (
    <>
      <ModelView navigation={navigation} />
    </>
  );
};

export default Model;
